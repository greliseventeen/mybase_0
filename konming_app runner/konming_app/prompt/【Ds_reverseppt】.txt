【-1】
整合归纳以下两个重点：
（1）内容详情播放时将原本位于页面中央的播放（暂停）菜单栏集成到播放进度条的下方作为播放状态栏，（不点击时默认隐藏）同时支持播放时单击屏幕暂停视频
（2）检查各文件的逻辑关系及文件内的配置，避免后续出现发布评论后播放视频闪退的bug。同时将评论发送栏位置设置为始终在可见屏幕的最底部，在评论与内容&帖子详情卡片之间增加一道有一定宽度的白色带灰色边框区域用于分隔

【-2】
根据以下内容微调生成的详细扩展提示词。
【内容】整合以下开发者提供的其他 额外扩展功能到批次2 生成的详细扩展提示词，参考bilibili相应页面设计自行补充完善功能具体描述：
（1）社区页：----帖子支持显示头像和用户昵称（不显示用户id）
         -----浏览帖子时支持显示标题，支持评论
（2）上传、发布页：
-----发布帖子时支持设置标题
-----上传内容页面始终无法选择分区（选择分区栏固定且为硬编码），需要改为下拉小菜单可选择，并支持用户自定义分区，同时需要更新主页的分区，当前仅支持选择一个分区
 （3）主页：优化显示布局，去除各卡片上下空白间隔，将此空间用于增加内容封面的高度（长高比可略微调整）

【-3】
生成全局提示词，加入以下子任务：在上传功能的默认封面，将视频类的默认封面设置为视频的第一帧，同时对于其他类型的内容，支持用户自定义默认封面（新增设一个小按钮）

【-4】
生成批次2详细开发提示词

【-5】
再加入以下任务到提示词中：在全屏播放时将暂停菜单栏整合到隐藏状态栏而非直接在页面中间显示，避免遮挡，提升观看体验，然后生成批次1详细开发提示词，并在提示词中简要强调以下约束：1.调用新的库函数时的import 2.自动分类生成文件并创建相关的2级包

【0】将以下杂乱任务内容分类归纳整合，重新对扩展功能分批次规划，建议将各任务分为现有功能优化及bug修复和功能扩展两阶段进行（先进行批次1），
（1）先对于现阶段项目的各个模块优化重构进行分批次规划（逐模块逐页面逐功能检查，解决可能存在的潜在bug如闪退，同时考虑各模块之间的交互逻辑是否合理，是否有潜在bug，检查mvvm架构是否需要重构）
（2）参考上次对话，重新对推荐页（ui.home）扩展功能分批次规划
【具体任务内容】
--------------------------------
-------------【1】
整合归纳以下两个重点：
（1）内容详情播放时将原本位于页面中央的播放（暂停）菜单栏集成到播放进度条的下方作为播放状态栏，（不点击时默认隐藏）同时支持播放时单击屏幕暂停视频
（2）检查各文件的逻辑关系及文件内的配置，避免后续出现发布评论后播放视频闪退的bug。同时将评论发送栏位置设置为始终在可见屏幕的最底部，在评论与内容&帖子详情卡片之间增加一道有一定宽度的白色带灰色边框区域用于分隔
【2】
根据以下内容微调生成的详细扩展提示词。
【内容】整合以下开发者提供的其他 额外扩展功能到批次2 生成的详细扩展提示词，参考bilibili相应页面设计自行补充完善功能具体描述：
（1）社区页：----帖子支持显示头像和用户昵称（不显示用户id）
         -----浏览帖子时支持显示标题，支持评论
（2）上传、发布页：
-----发布帖子时支持设置标题
-----上传内容页面始终无法选择分区（选择分区栏固定且为硬编码），需要改为下拉小菜单可选择，并支持用户自定义分区，同时需要更新主页的分区，当前仅支持选择一个分区
 （3）主页：优化显示布局，去除各卡片上下空白间隔，将此空间用于增加内容封面的高度（长高比可略微调整）

【3】
生成全局提示词，加入以下子任务：在上传功能的默认封面，将视频类的默认封面设置为视频的第一帧，同时对于其他类型的内容，支持用户自定义默认封面（新增设一个小按钮）

【4】
生成批次2详细开发提示词

【5】
再加入以下任务到提示词中：在全屏播放时将暂停菜单栏整合到隐藏状态栏而非直接在页面中间显示，避免遮挡，提升观看体验，然后生成批次1详细开发提示词，并在提示词中简要强调以下约束：1.调用新的库函数时的import 2.自动分类生成文件并创建相关的2级包



----------
--------------------


【6】改分区选择&背景色美化&删除app后账号密码丢失

【7】
接下来请对于一、推荐页（ui.home）生成扩展提示词（根据难易度与相关性规划，选择是否分批次进行，先生成简洁的参考，后续分步生成详细提示词），选择功能为：（1）1.2（2） 1.9。
在此之前需要先进行整合由以下开发者提供的扩展功能，参考bilibili相应页面设计自行补充完善功能具体描述：
（1）美化主页：主页发布的内容显示发布者，同时适当增大卡片大小，要求单个内容卡片统一设置为封面图片卡片（16*9）+下方等宽度较短的基本信息卡片（发布者，标题），必须参考bilibili内容卡片格式

（2）定位并修复切换到其他导航页后返回主页上展示内容丢失的bug（具体表现为执行上述操作后返回主页时页面除了“下拉刷新”几个字其余变为空白）


（3）在主页音视频详情页和社区页帖子详情页中添加评论功能
----在评论展示页面最下方设置交互窗，点击即可发布评论，
----用户发布后保存到数据库，支持所有用户预览，并可以点赞，（记录并显示点赞数）支持删除功能。
-----评论展示：在评论卡片左上角显示缩小用户头像和用户名，同时显示发布时间支持选择按时间显示和按热度排序（暂时用点赞数代替）列表显示


（4）在主页音视频详情页和社区页帖子详情页中添加转发功能



（5）在主页右上角添加私信页面，支持与好友进行聊天
------1级子页面为好友列表，按发消息时间降序排列
------点击好友可进入聊天详情页

（6）在主页最上方和社区页最上方添加搜索功能
